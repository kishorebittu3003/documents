pipeline{
    agent {label 'ansible'}
    stages{
        stage('clonning'){
            steps{
                sh"""
                    cd integration_ansible_jenkins/ 
                    ansibile -i devhosts -m ping all
                    ansible-playbook -i devhosts playbook
                  """
            }
        }
    }
}
